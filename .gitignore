# Dependencies
node_modules/

# macOS junk
.DS_Store
**/.DS_Store

# Build artifacts (regenerated by GitHub Actions on every deploy)
images/**/*.webp
videos/*.webm

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
Thumbs.db

# Lighthouse local reports
.lighthouseci/
lhr-*.json
lhr-*.html

# Working files (audit, copy variants, etc.)
*.xlsx
.~lock.*#

# Backup files
*.bak

# Дотоод баримт — нийтийн сайтын repo-д ХЭЗЭЭ Ч бүү оруул.
# GitHub Pages нь repo-гийн БҮХ файлыг нийтэд server хийдэг (кодоос эш татагдаагүй ч).
n8n/
*.docx
stamp.png
*Гэрчилгээ*
apps-script*.gs
